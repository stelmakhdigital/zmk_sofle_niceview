# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

# Disable display on keyboard halves (display will be on dongle)
CONFIG_ZMK_DISPLAY=n

# Sleep configuration
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000
CONFIG_ZMK_SLEEP=y

# Encoders support
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Enable support to control external power output
CONFIG_ZMK_EXT_POWER=y

# RGB underglow support
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
# 5 minutes timeout for RGB auto-off
CONFIG_ZMK_IDLE_TIMEOUT=300000

# Configure as peripheral devices (keyboards connect to dongle)
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

# Disable USB on keyboard halves (they connect via BLE to dongle)
CONFIG_ZMK_USB=n

# Enable BLE for connection to dongle
CONFIG_ZMK_BLE=y

# Increase BT power for better range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable experimental BLE connection support
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# Enable KSCAN driver debug logs
CONFIG_KSCAN_LOG_LEVEL_DBG=n

